1. Color Tokens
Text:
text-primary → MUI text color
text-secondary → MUI secondary text color
text-disabled → MUI disabled text color
text-link → MUI link color
text-on-accent → MUI text color on accent backgrounds
Background:
background-default → MUI default background color
background-subtle → MUI subtle background color
background-accent → MUI accent background color
background-danger → MUI danger background color
background-success → MUI success background color
Border:
border-default → MUI default border color
border-focus → MUI focus border color
border-danger → MUI danger border color
2. Typography Tokens
font-body → MUI body font family
font-heading → MUI heading font family
font-code → MUI code font family
font-cta → MUI call-to-action font family
3. Spacing Tokens
spacing-stack-xs → MUI extra small spacing
spacing-stack-sm → MUI small spacing
spacing-inline-md → MUI medium inline spacing
spacing-page-padding → MUI page padding
4. Size / Sizing Tokens
avatar-size-sm → MUI small avatar size
button-height-lg → MUI large button height
5. Elevation / Shadow Tokens
shadow-sm → MUI small shadow
shadow-md → MUI medium shadow
shadow-lg → MUI large shadow
6. Border Radius Tokens
radius-sm → MUI small border radius
radius-md → MUI medium border radius
radius-pill → MUI pill border radius
7. State / Intent Tokens
color-success → MUI success color
color-warning → MUI warning color
color-danger → MUI danger color
color-info → MUI info color