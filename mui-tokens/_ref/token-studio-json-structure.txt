# Tokens Studio JSON Structure Reference (2024)

## 1. Top-Level Structure
- The top-level of the JSON is a set (e.g., "MUI", "core", "light", "dark").
- Each set is an object containing categories as direct children.

**Example:**
```json
{
  "MUI": {
    // categories go here
  }
}
```

## 2. Category Level
- Categories are direct children of the set.
- Category names are camelCase, singular, and never pluralized or wrapped.
- No extra nesting or wrappers.

**Example:**
```json
"spacing": { ... }
"borderRadius": { ... }
"boxShadow": { ... }
"palette": { ... }
```

## 3. Token Level
- Each token is a direct child of its category.
- Token names are semantic and meaningful (e.g., "sm", "lg", "primary", "main").

**Example:**
```json
"spacing": {
  "sm": { "$type": "dimension", "$value": "4" },
  "md": { "$type": "dimension", "$value": "8" }
}
```

## 4. Token Object Structure
- Each token object must have:
  - `$type`: The type of the token, matching the category (e.g., "color", "dimension", "borderRadius").
  - `$value`: The value, which can be a string, number, or reference.
  - `$description`: (Optional) Human-readable description.

**Example:**
```json
"primary": {
  "$type": "color",
  "$value": "#1976d2",
  "$description": "Primary brand color."
}
```

## 5. References
- References use curly braces and dot notation: `{category.tokenName}`
- Multi-value tokens use space-separated references: `{borderRadius.sm} {borderRadius.lg}`

## 6. No Extra Wrapping
- Never wrap categories or tokens in additional objects or arrays.
- Never use a "type" or "set" key at the top level—only the set name (e.g., "MUI").

## 7. No Forced or Empty Categories
- Only include categories and tokens that are present and needed.
- Do not add empty categories or tokens.

## 8. Full Example (2024)
```json
{
  "MUI": {
    "palette": {
      "primary": {
        "main": { "$type": "color", "$value": "#1976d2" },
        "light": { "$type": "color", "$value": "#42a5f5" }
      },
      "secondary": {
        "main": { "$type": "color", "$value": "#9c27b0" }
      }
    },
    "spacing": {
      "xs": { "$type": "dimension", "$value": "4" },
      "sm": { "$type": "dimension", "$value": "8" }
    },
    "borderRadius": {
      "sm": { "$type": "borderRadius", "$value": "4" },
      "lg": { "$type": "borderRadius", "$value": "8" }
    },
    "boxShadow": {
      "elevation1": { "$type": "boxShadow", "$value": "0 1px 2px 0 rgba(0,0,0,0.05)" }
    },
    "opacity": {
      "disabled": { "$type": "opacity", "$value": "0.38" }
    },
    "h1": {
      "$type": "typography",
      "$value": {
        "fontFamily": "Roboto",
        "fontSize": "6rem",
        "fontWeight": 300,
        "lineHeight": 1.167,
        "letterSpacing": "-0.01562em"
      }
    }
  }
}
```

## 9. Golden Rules
- Set name is the only top-level key.
- Categories are direct children of the set.
- Tokens are direct children of categories.
- Token objects have $type, $value, and optionally $description.
- No extra wrappers, no pluralization, no empty categories.
- Use semantic, meaningful names for tokens.
- Always validate your output against this reference before using or committing.
- If you add a new category or token type, update this reference with an example.
- If in doubt, compare your output to this file and to a known-good Tokens Studio export.

Instruction for Future Scripts/Developers
Always validate your output against this reference before using or committing.
If you add a new category or token type, update this reference with an example.
If in doubt, compare your output to this file and to a known-good Tokens Studio export.

Token Types in Tokens Studio
The type is the anatomic part of a Design Token that defines the category of design property the decision belongs to, or when it can be applied.  

A full Composite Design Token looks like this and this is the ultimate goal:


Fake.Font.Token renders as:
"Fake": {
      "Font": {
        "Token": {
          "$type": "typography",
          "$value": {
            "fontFamily": "Roboto",
            "fontSize": "{typography.body1.fontSize}",
            "fontWeight": "Regular",
            "letterSpacing": "{typography.body1.letterSpacing}",
            "lineHeight": "{typography.body1.lineHeight}",
            "paragraphSpacing": "Inherit",
            "paragraphIndent": "Inherit",
            "textDecoration": "none",
            "textCase": "{typography.button.textCase}"
          },
          "$description": "This is an example I'm pushing to Github. This is the update."
        }
      }
    }
```

## Canonical Example Reference
- The file `_ref/tokens-studio-canonical-example.json` is the golden source for all token structure, nesting, and $type usage.
- **All scripts and outputs must match the structure and nesting of this file.**
- Any changes to the output format must be reflected in this canonical file first.

**Example snippet:**
```
"Padding": {
  "Lg": {
    "$type": "sizing",
    "$value": "24px",
    "$description": "This is a null token that is an example to correct Cursor and not to be used for real."
  },
  ...
}
```

**Validation:**
- Always compare script output to `_ref/tokens-studio-canonical-example.json` before using or committing.
- If the output does not match, update the script (not the canonical file) unless you are intentionally changing the format.